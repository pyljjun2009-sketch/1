fix: 审核修复 -- 写操作容错 + manifest 作用域 + 体验经验代码化

代码质量:
- backup.js: 备份写入操作加 try-catch（磁盘满/权限拒绝时清理半完成目录）
- crash-recovery.js: markCleanExit/incrementCrashCount 写操作加 try-catch
- backup.js: manifest 变量作用域移出 try-catch（修复 ReferenceError）

经验落地:
- DSH 全局安装完整性预检（yaml schema 模块验证）
- Cannot find module 诊断分流
- 卸载不完整判定升级为 error（阻断启动）
- 五项一致性检查 + 设置页 Profile 健康检查按钮
- DSH-启动卡住经验总结.md

总计: 65/65 测试通过, 23/23 语法通过
