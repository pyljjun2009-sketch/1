feat: bundle 安装一致性防护 + 修复插件依赖按钮

基于 SSH 插件安装不一致经验（package.json 声明但 node_modules 缺失）：

1. 启动预检：验证每个 bundle 在 node_modules 实际存在，缺失时给出
   插件名 + dsh plugin install 修复指引
2. 错误诊断：cannot resolve profile bundle 时提取具体 bundle 名
3. 设置页新增"修复插件依赖"按钮：执行 dsh plugin install 同步状态
4. 新增 _checkProfileBundles 单元测试

总计: 63/63 测试通过
